// ProductFilters component
// Wire up with category list, price range, sort options
// Used on /products page alongside ProductGrid

'use client';
export default function ProductFilters({ filters, onFilterChange }) {
  return (
    <aside>
      {/* Categories, price range, sort — implement as needed */}
      <p style={{ color: 'var(--color-text-muted)', fontSize: 'var(--font-size-sm)' }}>
        Filters component — ready to implement
      </p>
    </aside>
  );
}
